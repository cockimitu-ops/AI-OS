# report_builder.py's only dependency. Not installed anywhere on the server -
# building a report PDF needs this first, in a venv:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#   .venv/bin/python report_builder.py reports/omni_shield.py
reportlab>=4.0
